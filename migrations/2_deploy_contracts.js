const BitherToken = artifacts.require("./BitherToken.sol")
const BitherStockToken = artifacts.require("./BitherStockToken.sol")
const BitherCrowdsale = artifacts.require("./BitherCrowdsale.sol")
// const time = require("openzeppelin-solidity/test/helpers/time")

module.exports = async (deployer, network, accounts) => {
    // await deployer.deploy(BitherToken)
    // await deployer.deploy(BitherStockToken)

    // bitherTokensOwner = accounts[0]
    // etherBenefactor = accounts[5]
    // openingTime = await time.latest()
    // await BitherCrowdsale.new(BitherToken.address, BitherStockToken.address, bitherTokensOwner, etherBenefactor, openingTime)
}
